package newspaperoot.ui;

public class CredentialUI {

    @Inject// es mas eficiente va en la clase que va unido a lo quese inicializa en el main
    public boolean checLogin(CredentialUI credentialUI) {
        return false;
    }
}
